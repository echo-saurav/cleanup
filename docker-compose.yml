services:
  cleanup:
    container_name: cleanup
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=8888
    ports:
      - "8887:8888"
    volumes:
      - ./data:/data

